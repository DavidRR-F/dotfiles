[global]
frame_color = "{{ blue }}"
separator_color= frame
highlight = "{{ blue }}"

[urgency_low]
background = "{{ base }}"
foreground = "{{ text }}"

[urgency_normal]
background = "{{ base }}"
foreground = "{{ text }}"

[urgency_critical]
background = "{{ base }}"
foreground = "{{ text }}"
frame_color = "{{ peach }}"
